{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y git-lfs && git lfs install && git lfs pull --include=\"Leiden/finetuned_sentiment_cardiff_xlmroberta/**,Leiden/pipeline_out/**\" && pip install -r Leiden/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd Leiden && python main_gui.py",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
